{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Attach to Maven Cucumber Test",
            "request": "attach",
            "hostName": "localhost",
            "port": 5005,  // The port that Maven's JVM will listen on for debugging
            "projectName": "sauce-demo-tests"
        }
    ]
}